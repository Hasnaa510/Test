import 'package:flutter/material.dart';

class Science2 extends StatelessWidget {
  const Science2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Science Screen'),
    );
  }
}