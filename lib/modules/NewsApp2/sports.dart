import 'package:flutter/material.dart';

class Sports2 extends StatelessWidget {
  const Sports2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Sports Screen'),

    );
  }
}